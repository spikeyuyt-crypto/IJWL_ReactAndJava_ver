<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ijwl_javabackend.mapper.A001SignInAndRegisterMapper">
    <select id="getUserPassword" resultType="java.lang.String">
        SELECT password FROM users
        WHERE user_name = #{username}
    </select>

    <select id="getUserSettings" resultType="com.example.ijwl_javabackend.entity.A001LogInListBean">
        SELECT users.user_id, user_settings.font_size, user_settings.background_color
        FROM users
        JOIN user_settings ON users.user_id = user_settings.user_id
        WHERE users.user_name = #{username}
    </select>

    <select id="checkUsername" resultType="java.lang.Boolean">
        SELECT EXISTS (SELECT 1 FROM users WHERE user_name = #{username})
    </select>

    <insert id="registerUser"
            useGeneratedKeys="true"
            keyProperty="user.userId">
        INSERT INTO users (user_name, password)
        VALUES (#{user.username}, #{password})
    </insert>

    <insert id="registerUserSettings">
        INSERT INTO user_settings (user_id, font_size, background_color)
        VALUES (#{userId}, #{fontSize}, #{backgroundColor})
    </insert>

</mapper>
